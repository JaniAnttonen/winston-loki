syntax = "proto3";

package logproto;

message PushRequest {
  repeated Stream streams = 1;
}

message Stream {
  string labels = 1;
  repeated Entry entries = 2;
}

message Entry {
  string timestamp = 1;
  string line = 2;
}
